.env
node_modules/
public/uploads/*
!public/uploads/.gitkeep
# フィット済み GLB はチームで共有するためコミットする。
# （名前付きボリュームをやめて bind mount にしたのはこのため。
#   docker-compose.yaml の api サービスのコメント参照）
# Gemini の中間画像は承認UI用の使い捨てなので共有しない。
public/generated/*
!public/generated/.gitkeep
!public/generated/*.glb
