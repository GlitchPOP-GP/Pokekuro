.env
node_modules/
public/uploads/*
!public/uploads/.gitkeep
# generated/ は実行時の生成物置き場（bind mount）。共有しない。
# チームやレビュアーに渡したいデモ用 GLB は public/models/ に置くこと。
# models/ は bind mount されないので api イメージに焼き込まれ、
# イメージだけ渡しても表示できる。
public/generated/*
!public/generated/.gitkeep
