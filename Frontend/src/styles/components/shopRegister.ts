import { StyleSheet } from "react-native";

export const shopRegisterStyles = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "rgba(255, 255, 255, 0.8)",
    borderRadius: 12,
    borderWidth: 1,
    borderColor: "#e5d5c5",
    paddingHorizontal: 16,
    height: 50,
  },
  input: {
    flex: 1,
    fontSize: 16,
    color: "#111",
    fontWeight: "500",
  },
  mockButton: {
    padding: 8,
  },
});
